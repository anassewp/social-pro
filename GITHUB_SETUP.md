# تعليمات رفع المشروع على GitHub

## الخطوة 1: إعداد Git (إذا لم يكن مُعداً من قبل)

قم بتشغيل هذه الأوامر في Terminal:

```bash
# إعداد اسم المستخدم (استبدل "Your Name" باسمك)
git config --global user.name "Your Name"

# إعداد البريد الإلكتروني (استبدل "your.email@example.com" ببريدك)
git config --global user.email "your.email@example.com"
```

## الخطوة 2: إنشاء المستودع الأولي (تم تنفيذها بالفعل ✓)

تم تهيئة Git وإضافة الملفات بنجاح.

## الخطوة 3: إنشاء Commit الأول

قم بتشغيل:

```bash
git commit -m "Initial commit: Social Pro application"
```

**ملاحظة:** إذا ظهرت رسالة خطأ حول تكوين الهوية، قم بتنفيذ الخطوة 1 أولاً.

## الخطوة 4: إنشاء مستودع على GitHub

1. اذهب إلى [GitHub.com](https://github.com) وسجل الدخول
2. اضغط على زر **"+"** في الزاوية العلوية اليمنى
3. اختر **"New repository"**
4. أدخل اسم المستودع (مثل: `social-pro`)
5. اختر **Private** أو **Public** حسب تفضيلك
6. **لا تقم** بإنشاء README أو .gitignore (لأننا بالفعل لدينا ملفات)
7. اضغط **"Create repository"**

## الخطوة 5: ربط المشروع المحلي بـ GitHub

بعد إنشاء المستودع على GitHub، ستجد صفحة تحتوي على تعليمات. استخدم الأوامر التالية:

```bash
# إضافة المستودع البعيد (استبدل USERNAME و REPO_NAME)
git remote add origin https://github.com/USERNAME/REPO_NAME.git

# التحقق من إضافة المستودع البعيد
git remote -v
```

## الخطوة 6: رفع المشروع إلى GitHub

```bash
# رفع المشروع (استبدل main بـ master إذا كان فرعك الرئيسي master)
git branch -M main
git push -u origin main
```

إذا كان اسم الفرع الرئيسي `master`، استخدم:
```bash
git push -u origin master
```

## ملاحظات مهمة:

1. **ملفات .env:** تأكد من أن ملفات البيئة (`.env*`) موجودة في `.gitignore` (وهي موجودة بالفعل)
2. **المعلومات الحساسة:** لا ترفع أي مفاتيح API أو كلمات مرور في الكود
3. **node_modules:** مجلد `node_modules` سيتم تجاهله تلقائياً بواسطة `.gitignore`

## في حالة وجود مشاكل:

### إذا تم رفض Push:
```bash
# ربما تحتاج لمصادقة GitHub
git push -u origin main
```

### إذا كنت تستخدم SSH بدلاً من HTTPS:
```bash
git remote add origin git@github.com:USERNAME/REPO_NAME.git
```

### للتحقق من حالة المستودع:
```bash
git status
```

---

**بعد إتمام الخطوات، سيتم رفع مشروعك بنجاح على GitHub! 🎉**

